# etc config
sudo vi /etc/hostname                               # Modify the host name
sudo vi /etc/network/interfaces                     # Set a static IP address
sudo vi /etc/hosts                                  # Modify hosts
